spring:
  exposed:
    generate-ddl: true
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/chilpost_sb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    problemdetails:
      enabled: true
server:
  error:
    whitelabel:
      enabled: false
